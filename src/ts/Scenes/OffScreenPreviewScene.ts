import Scene from "../Scene";
import SceneManager from "../SceneManager";

export default class OffScreenPreviewScene extends Scene {
    constructor(sceneManager:SceneManager)
    {
        super(sceneManager);
    }

    public init():void
    {

    }
}